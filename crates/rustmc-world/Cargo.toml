[package]
name = "rustmc-world"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Durable voxel block store for RustMC (place/break equivalent set+get with reload)"
readme = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
